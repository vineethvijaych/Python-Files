# 3. Create a module named "employee" which contains a class named "Employee"
#  with the following attributes and methods:
#  Attributes:
# i. name: Name of the employee
# ii. age: Age of the employee
# iii. salary: Salary of the employee
#  Methods:
# iv. get_name(): Returns the name of the employee
# v. get_age(): Returns the age of the employee
# vi. get_salary(): Returns the salary of the employee
# Import the module in a separate Python script and create some employee objects. Use
# the methods to retrieve their attributes. 

import employee as emp

emp.a.get_age() 
emp.a.get_name()
emp.a.get_salary()
print(emp.a.b)
